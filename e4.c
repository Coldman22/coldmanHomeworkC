//C program to print ASCII values of a character 
#include <stdio.h>
int main()
{
    char ch;

    printf("Enter the character: ");
    scanf("%c", &ch);

    printf("ASCII value is %d\n", ch);
    return 0;
}